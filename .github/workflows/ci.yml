name: CI
on:
  push:
  pull_request:

jobs:
  static:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python scripts/preflight.py
      - run: python -m compileall contracts tests scripts
      - run: pip install pytest
      - run: pytest -q tests/unit
